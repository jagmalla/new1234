// Content script — Module 1 placeholder.
// <video>/<source> scanning, MSE/blob detection and EME (DRM) flagging arrive in Module 2.
// Present now only so the manifest's content_scripts entry resolves to a real file.

console.log('[JIM Video Catcher] content script loaded on', location.host);

export {};
